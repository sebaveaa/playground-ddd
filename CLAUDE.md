Claude agents should use `AGENTS.md` as the source of truth for repo instructions.

Do not duplicate agent operating rules here. If Claude-specific behavior is needed later, add only the delta in this file and keep shared guidance in `AGENTS.md`.
